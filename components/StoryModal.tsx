interface Props {
  image: string
  userName: string
  setShowModal: React.Dispatch<React.SetStateAction<boolean>>
}

const StoryModal: React.FC<Props> = ({ image, userName, setShowModal }) => {
  return <div></div>
}

export default StoryModal
