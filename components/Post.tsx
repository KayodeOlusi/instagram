interface Props {
  id: any
  userName: string
  img: string
  caption: string
  userImage: string
}

const Post: React.FC<Props> = ({ id, userName, img, caption, userImage }) => {
  return <div></div>
}

export default Post
